# Supabase - same values as backend .env
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Simple password to access the dashboard
DASHBOARD_PASSWORD=changeme

# FastAPI backend URL (for practice/test mode)
BACKEND_URL=http://localhost:8000
